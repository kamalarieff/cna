/*
 *
 * {{ properCase name }} reducer
 *
 */
 
import { DEFAULT_ACTION } from './constants';
import { {{ properCase name }}Actions, IState } from './types';

const initialState: IState = {};

export default function reducer (state: IState = initialState, action: {{ properCase name }}Actions) {
  switch (action.type) {
    case DEFAULT_ACTION:
      return {...state};
    default:
      return state;
  }
}
